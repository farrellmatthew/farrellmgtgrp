# FMG Site

Static HTML brand preview for Matt Farrell.

## Open

Open [index.html](/Users/matt.farrell/Library/CloudStorage/Dropbox/__Book%20-%20Teach%20the%20Game/Website/FMG/index.html) in a browser or serve the `FMG` folder with any simple static server.

## Password Gate

The page uses a client-side password overlay and blur state. This is suitable for a lightweight preview gate, but the password still exists in the page source because the site is static HTML.
